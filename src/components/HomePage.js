import React from "react";
import { CardList, NavBar } from "../utils/constanst";

const HomePage = () => {
  return (
    <div>
      <NavBar />
      <CardList />
    </div>
  );
};

export default HomePage;
